{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.py) if [ -x .venv/bin/ruff ]; then .venv/bin/ruff check --fix \"$f\"; elif command -v ruff >/dev/null 2>&1; then ruff check --fix \"$f\"; fi ;; esac; } 2>/dev/null || true",
            "statusMessage": "ruff check --fix"
          }
        ]
      }
    ]
  }
}
